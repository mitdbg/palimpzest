""" An Implementation Strategy is a class tasked with creating a
    specific type of implementation for a given Physical Op.
    Example: a "filter" physical op may have a "filter" implementation for naive LLM,
    a "filter" implementation for LLM with a bloom filter, etc.
     
"""
