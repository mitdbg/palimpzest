You are bootstrapping a traversal ranker for an adaptive graph search.

Query:
{{ query }}

Write concise criteria for scoring a candidate node's relevance to the query.
- Focus on topical relevance and likely evidence value.
- Prefer nodes that are specific, actionable, and contain operational details.

Return plain text criteria (no JSON).