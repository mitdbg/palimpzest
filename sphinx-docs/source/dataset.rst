Dataset
=======

The `Dataset` class is the building block for composing semantic pipelines. Users create an initial `Dataset` which is then transformed by calls to operators.

.. autosummary::
   :toctree: generated

   palimpzest.sets
